type Query {
  getCats(id: ID!): Cat
  cat(id: ID!): Cat
  getOne(id:ID!):Contents
}

# type Mutation {
#   createCat(createCatInput: CreateCatInput): Cat
# }

# type Subscription {
#   catCreated: Cat
# }

type Cat {
  id: Int
  name: String
  age: Int
}
type Contents{
  id:Int
  title:String
  category: String
  authors: String
  content: String
  time:String
  img: String
  status: String
  description: String
}
# input CreateCatInput {
#   name: String
#   age: Int
# }
